// C++ code
//
int LED = 1;
int Pushbutton = 2;
void setup()
{
  pinMode(LED, OUTPUT);
  pinMode(2, INPUT);
}
 
void loop()
{
  if (digitalRead(2) == LOW) {
    digitalWrite(11, HIGH);
  }
  else {
    digitalWrite(LED, LOW);
  }
 
}
